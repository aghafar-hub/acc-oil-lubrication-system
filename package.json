{
  "name": "acc-oil-lubrication-system",
  "private": true,
  "version": "0.2.0",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "deploy": "npm run deploy --workspace=frontend"
  }
}
